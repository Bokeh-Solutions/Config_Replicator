The script files should have a ".lst" extension the format of teh file should be:

IP_ADDRESS DEVICE_NAME (telnet/ssh)

if telnet/ssh is not present or it is not the words 'telnet' or 'ssh' then it is used telnet by default

Use the directive "!desc:" to add a description about the list